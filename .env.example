# Optional: Ollama server URL (default: http://localhost:11434)
# OLLAMA_HOST=http://localhost:11434

# Optional: Path to iVISPAR repo after cloning (for run_benchmark --benchmark ivispar)
# IVISPAR_ROOT=../iVISPAR
